import { createTheme } from "@mui/material";

export const appTheme = createTheme({
  typography: {
    fontFamily: "system-ui",
  },
  palette: {
    primary: {
      main: "#0070f3",
    },
    background: {
      default: "#F5F5F5",
    },
  },
});
